server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/best?useSSL=true&characterEncoding=utf8&serverTimezone=GMT%2B8
    username: root
    password: 123456
    #    driver-class-name: com.mysql.cj.jdbc.Driver
    #    schema: classpath:best.sql

upload:
  relativePath: /files/
  realPath: C:/devFile/

logging:
  file:
    name: log/best.log
  level:
    org.shanzhaozhen.bestrepo.mapper: debug
